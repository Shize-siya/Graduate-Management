server:
  port: 8080

spring:
  application:
    name: graduate-management
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/graduate_management?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 336891ty
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: graduateManagementSecretKey2024VeryLongSecretKeyForJWTTokenGeneration
  expiration: 86400000
